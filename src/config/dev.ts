import { IKeys } from "./keys";

export const keys: IKeys = {
  JWT_SECRET: "",
  CLIENT_ORIGIN: "http://localhost:3000",
  PGHOST: "localhost",
  PGPORT: "5432",
  PGDATABASE: "",
  PGUSER: "michal",
  PGPASSWORD: "",
};
